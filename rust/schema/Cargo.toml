[package]
name = "schema"
version = "0.1.0"
edition = "2021"

[features]
llguidance = []

[dependencies]
serde_json = { version = "1.0", features = ["preserve_order"] }
regex = "1.10"
once_cell = "1.19"
indexmap = "2.2"
thiserror = "1.0"

